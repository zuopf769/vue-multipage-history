import Vue from 'vue'
import Router from 'vue-router'
import {{=it.compname}} from '@/pages/{{=it.name}}/home/home'

Vue.use(Router)

export default new Router({
  mode: 'history',
  routes: [
    {
      path: '/',
      name: '{{=it.compname}}',
      component: {{=it.compname}}
    }
  ]
})
